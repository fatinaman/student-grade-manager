#ifndef STATISTICS_H
#define STATISTICS_H

#include "student.h"

float getHighest(Student *students, int count); // returns the highest marks
float getAverage(Student *students, int count); // returns the average marks
#endif